import { Express } from 'express';
import { handleError } from 'req-error';
import { GetBodyMethod } from "./getBody.cjs";
import { SussessMethod } from "./success.cjs";
import 'req-error/global';
declare global {
    namespace Express {
        interface Response {
            success: SussessMethod;
        }
        interface Request {
            getBody: GetBodyMethod;
        }
    }
}
declare const _default: (app: Express, { ping, errorMsgs, formatJSON }?: ConfigOptions) => void;
export default _default;
export { default as checkType } from "./checkType/index.cjs";
export { GetBodyMethod, SussessMethod };
export type ConfigOptions = {
    ping?: string;
    errorMsgs?: Parameters<typeof handleError>[1];
    formatJSON?: Parameters<typeof handleError>[2];
};
