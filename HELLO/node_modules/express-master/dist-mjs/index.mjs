import { handleError } from 'req-error';
import getBodyMethod from "./getBody.mjs";
import successMethod from "./success.mjs";
import pingController from "./ping.mjs";
import 'req-error/global';
export default (app, { ping, errorMsgs, formatJSON } = {}) => {
    app.response.success = successMethod;
    app.request.getBody = getBodyMethod;
    if (ping && typeof ping === 'string')
        app.use(ping, pingController);
    handleError(app, errorMsgs, formatJSON);
};
export { default as checkType } from "./checkType/index.mjs";
